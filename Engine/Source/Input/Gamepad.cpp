/*
#include "nle_pch.hpp"
#include "Gamepad.hpp"
#include "Input.hpp"

namespace nle
{
	

	Gamepad Input::GetGamepad(int id)
	{
		if (id < 0)
			id = 0;
		else if (id > gamepads.size() - 1)
			id = gamepads.size() - 1;

		return gamepads[id];
	}

	void Input::AddGamepad()
	{
	}
}
*/